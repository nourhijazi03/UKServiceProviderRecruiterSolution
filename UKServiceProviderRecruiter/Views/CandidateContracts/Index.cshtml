@model IEnumerable<UKServiceProviderRecruiter.CandidateContract>

@{
    ViewBag.Title = "Index";
}
<style>

    .fa {
        display: inline-block;
        font: normal normal normal 14px/1.5 FontAwesome !important;
        font-size: inherit;
        text-rendering: auto;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
    }
</style>

<br />


<div class="box box-primary">

    <div class="box-header with-border">
        <h3 class="box-title"><i class="fa fa-list-alt"></i> <span>Candidate Contract</span></h3>
        <div class="box-tools pull-right">
        </div>

    </div>


    @using (Html.BeginForm())
    {
        <div class="text-left">
            @Html.ActionLink("Create New", "Create")

            @*<button type="submit" id="btn-export-excel" class="btn btn-success btn-sm">Export To Excel</button>*@

        </div>

        <div class="box-body no-padding">

            <div class="mailbox-controls">
                <div id="DataTables_Table_0_wrapper" class="dataTables_wrapper form-inline dt-bootstrap no-footer">
                    <table id="demoGrid_info" class="table table-striped table-bordered dt-responsive " width="100%" cellspacing="0">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.TotalAmount)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.StatementDate)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.CandidateContractInvoice.CCID)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TotalAmount)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.StatementDate)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.CandidateContractInvoice.CCID)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.CandidateContractID }) |
                                        @Html.ActionLink("Details", "Details", new { id = item.CandidateContractID }) |
                                        @Html.ActionLink("Delete", "Delete", new { id = item.CandidateContractID })
                                    </td>
                                </tr>
                            }
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    }
</div>
<div class="box-footer no-padding">

</div>
<script type="text/javascript">



    $('#demoGrid_info').DataTable({
        "paging": true,
        "pageLength": 10,
        "pagingType": "full_numbers",
        "scrollX": true,

    });
</script>
